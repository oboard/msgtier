import {
  "moonbit-community/zipc",
}

options(
  "pre-build": [
    {
      "command": ":embed -i $input -o $output --binary",
      "input": "dist.zip",
      "output": "dist.zip.mbt",
    },
  ],
)
